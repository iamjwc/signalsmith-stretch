{
	"name": "signalsmith-stretch-web-dev",
	"private": true,
	"version": "0.0.0",
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"watch": "tsc -w -p tsconfig.json"
	},
	"devDependencies": {
		"typescript": "^5.6.3"
	}
}
